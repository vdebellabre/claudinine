# .NET build output (release binaries under bin/<rid>/ ARE committed — see plan)
obj/
src/**/bin/
tests/**/bin/
*.user
.vs/
.idea/
publish/

# Local benchmarking only, never committed
tests/POC reference/
