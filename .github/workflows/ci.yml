name: Commit validation

# Continuous integration: build, test and verify the packed archive on every
# push and PR. Publishes nothing -- releasing is cd.yml, a manual dispatch.
on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    uses: ./.github/workflows/build.yml
